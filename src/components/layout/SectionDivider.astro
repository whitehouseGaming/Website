---
export interface Props {
  flip?: boolean;
  class?: string;
}

const { flip = false, class: className = '' } = Astro.props;
---

<div
  class:list={[
    'h-16 w-full sm:h-24',
    flip ? 'diagonal-divider-rev' : 'diagonal-divider',
    className,
  ]}
  aria-hidden="true"
>
</div>
